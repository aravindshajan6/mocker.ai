# Mocker frontend

Next.js 16 app. See the root README for how to run the whole stack with Docker.

Local dev (backend on localhost:8000): `npm install && npm run dev`, then open http://localhost:3000.
